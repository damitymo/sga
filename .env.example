# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=changeme
DB_NAME=sga

# JWT
# Generate a strong secret:  node -e "console.log(require('crypto').randomBytes(64).toString('base64url'))"
JWT_SECRET=replace-me-with-a-strong-random-secret-at-least-32-chars

# Server
PORT=3001

# CORS (URL of the deployed frontend, e.g. https://sga-front.vercel.app)
FRONTEND_URL=http://localhost:3000
